curl -XGET 'http://localhost:9200/network/host/_search?routing=kimchy' -d '{
    "query": {
        "filtered" : {
            "query" : {
                "query_string" : {
                    "query" : "linux"
                }
            },
            "filter" : {
                "term" : {"ports[vulnerable]": "very-yes" }
            }
        }
    }
}
'
